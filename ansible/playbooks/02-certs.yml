- name: Generate Kubernetes PKI locally
  hosts: all
  gather_facts: false
  become: false
  any_errors_fatal: true
  vars:
    generated_dir: "{{ playbook_dir }}/../generated"
    pki_dir: "{{ generated_dir }}/pki"
    kubeconfig_dir: "{{ generated_dir }}/kubeconfigs"
  roles:
    - certs
